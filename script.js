
const startTimerBtn = document.getElementById('start-btn');

startTimerBtn.addEventListener('click',(event)=>{
    event.preventDefault();
    const hours = parseInt(document.getElementById('hour').value);
    const minutes = parseInt(document.getElementById('minute').value);
    const seconds = parseInt(document.getElementById('second').value);

})